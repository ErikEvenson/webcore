language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-example' ]; then cd .. && eval "mv $currentfolder generator-example" && cd generator-example; fi
